# Flutter Android Configuration
# This file disables Vulkan to prevent ErrorDeviceLost issues

# Force OpenGL ES backend instead of Vulkan
flutter.impeller.enable_vulkan=false
flutter.rendering.backend=opengles

# Alternative fix: Use software rendering for debugging
# flutter.rendering.backend=software